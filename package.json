{
  "name": "elm-ninja",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "styles": "sass src/styles/main.scss dist/style.css",
    "build": "elm make src/Main.elm --output dist/elm.js",
    "prod": "elm make src/Main.elm --optimize --output dist/elm.js && yarn styles",
    "format": "elm-format src/",
    "watcher": "watch 'yarn styles' src/styles & watch 'yarn build' src --ignoreDirectoryPattern=/src/styles/"
  },
  "devDependencies": {
    "bootstrap": "^4.5.2",
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-upgrade": "^0.19.8",
    "sass": "^1.26.10",
    "watch": "^1.0.2"
  }
}
